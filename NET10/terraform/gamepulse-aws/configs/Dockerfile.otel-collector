# OpenTelemetry Collector 커스텀 이미지
FROM otel/opentelemetry-collector-contrib:latest

# 구성 파일 복사
COPY otel-collector-docker.yaml /etc/otelcol-contrib/otel-collector-config.yaml

# 헬스 체크 설정
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:13133/health || exit 1

# 포트 노출
EXPOSE 4317 4318 8888 13133

# 실행 명령
CMD ["--config=/etc/otelcol-contrib/otel-collector-config.yaml"]